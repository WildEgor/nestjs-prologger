{
  "maxWorkers": 1,
  "moduleFileExtensions": ["ts", "js"],
  "testMatch": [
    // unit tests
    "<rootDir>/src/**/*.spec.ts",
    // e2e tests
    "<rootDir>/tests/**/*.spec.ts"
  ],
  "transform": {
    "^.+\\.ts$": "ts-jest"
  }
}
